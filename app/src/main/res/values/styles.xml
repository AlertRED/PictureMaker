<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <picture name="colorPrimary">@color/colorPrimary</picture>
        <picture name="colorPrimaryDark">@color/colorPrimaryDark</picture>
        <picture name="colorAccent">@color/colorAccent</picture>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <picture name="android:windowBackground">@drawable/splash_screen</picture>
    </style>

</resources>